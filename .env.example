# ============================================================================
# PLANTILLA DE VARIABLES DE ENTORNO - MIA V4.0
# ============================================================================
# 
# INSTRUCCIONES:
# 1. Copiar este archivo como ".env" en el directorio raíz del proyecto
# 2. Completar los valores requeridos (marcados con <REQUERIDO>)
# 3. Los valores opcionales pueden dejarse comentados (#)
# 4. NUNCA subir el archivo .env a control de versiones
#
# ============================================================================

# ----------------------------------------------------------------------------
# GEMINI AI CONFIGURATION
# ----------------------------------------------------------------------------
# API Key de Google Gemini para análisis de oportunidades
# Obtener en: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=<REQUERIDO>

# Modelo de Gemini a utilizar (opciones: gemini-pro, gemini-flash-latest)
# gemini-flash-latest es más rápido y económico (recomendado para Stage 1)
GEMINI_MODEL=gemini-flash-latest

# Límite de tokens por request (opcional, default: 10000)
# GEMINI_MAX_TOKENS=10000

# Número de intentos de retry en caso de error de API (default: 3)
# Usa backoff exponencial: espera 1s, 2s, 4s entre intentos
GEMINI_RETRY_ATTEMPTS=3

# ----------------------------------------------------------------------------
# GOOGLE SHEETS CONFIGURATION (Opcional - Para Fase 4)
# ----------------------------------------------------------------------------
# Ruta al archivo de credenciales de Google Service Account
# GOOGLE_SHEETS_CREDENTIALS_PATH=data/service_account.json

# ID del Google Spreadsheet donde se escribirán resultados
# GOOGLE_SHEETS_SPREADSHEET_ID=

# Nombre de la hoja donde se escribirán los datos
# GOOGLE_SHEETS_WORKSHEET_NAME=Oportunidades

# ----------------------------------------------------------------------------
# SCRAPING CONFIGURATION
# ----------------------------------------------------------------------------
# Timeout para requests HTTP en segundos (default: 15)
SCRAPER_TIMEOUT=15

# Número de reintentos en caso de fallo (default: 3)
SCRAPER_MAX_RETRIES=3

# Delay entre requests en segundos (para evitar bloqueos)
SCRAPER_DELAY_SECONDS=2

# User-Agent para requests HTTP (opcional)
# SCRAPER_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36

# ----------------------------------------------------------------------------
# LOGGING CONFIGURATION
# ----------------------------------------------------------------------------
# Nivel de logging (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# DEBUG: Máximo detalle, útil para desarrollo y debugging
# INFO: Información general de operaciones (recomendado)
# WARNING: Solo advertencias y errores
# ERROR: Solo errores críticos
LOG_LEVEL=INFO

# Tamaño máximo del archivo de log en MB antes de rotación
# Cuando el archivo alcanza este tamaño, se crea un backup y se inicia uno nuevo
LOG_ROTATION_SIZE_MB=10

# Número de archivos de backup de log a mantener
# Ejemplo: Con 5 backups, se mantienen main.log + 5 archivos históricos
LOG_BACKUP_COUNT=5

# Formato de logs en JSON (true/false)
# JSON facilita parsing automático para integración con sistemas de monitoreo
# Dejar en false para formato texto legible por humanos
JSON_LOGS=false

# ----------------------------------------------------------------------------
# OUTPUT CONFIGURATION
# ----------------------------------------------------------------------------
# Ruta al archivo CSV de salida (default: results_stage1.csv)
OUTPUT_CSV_FILE=results_stage1.csv

# Crear backup del CSV antes de cada ejecución (true/false)
OUTPUT_CREATE_BACKUP=true

# Directorio para backups (opcional)
# OUTPUT_BACKUP_DIR=backups

# ----------------------------------------------------------------------------
# SCHEDULER CONFIGURATION (Para Fase 5)
# ----------------------------------------------------------------------------
# Habilitar ejecución automática (true/false)
# SCHEDULER_ENABLED=false

# Frecuencia de ejecución en horas (default: 24)
# SCHEDULER_INTERVAL_HOURS=24

# Hora de inicio de ejecución (formato 24h: HH:MM)
# SCHEDULER_START_TIME=08:00

# ----------------------------------------------------------------------------
# PERFORMANCE CONFIGURATION
# ----------------------------------------------------------------------------
# Número máximo de portales a procesar en paralelo (default: 1)
# MAX_PARALLEL_PORTALS=1

# Habilitar caché de respuestas de Gemini (true/false)
# ENABLE_GEMINI_CACHE=false

# Tiempo de vida del caché en horas (default: 24)
# CACHE_TTL_HOURS=24

# ----------------------------------------------------------------------------
# DEVELOPMENT/DEBUG CONFIGURATION
# ----------------------------------------------------------------------------
# Modo de desarrollo (true/false) - más logging, sin rate limiting
# DEV_MODE=false

# Modo de prueba - solo procesar N portales (0 = todos)
# TEST_MODE_MAX_PORTALS=0

# Modo dry-run - no escribir resultados (true/false)
# DRY_RUN=false

# ----------------------------------------------------------------------------
# NOTIFICATION CONFIGURATION (Para Fase 4)
# ----------------------------------------------------------------------------
# Email para notificaciones (opcional)
# NOTIFICATION_EMAIL=

# SMTP Server para envío de emails (opcional)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=

# ----------------------------------------------------------------------------
# BUSINESS RULES
# ----------------------------------------------------------------------------
# Score mínimo de IA para considerar oportunidad relevante (0-100)
MIN_RELEVANCE_SCORE=40

# Rubros a incluir en búsqueda (separados por coma, vacío = todos)
# ACTIVE_RUBROS=Purificación - Ingeniería,Purificación - Provisión

# Portales a excluir (separados por coma)
# EXCLUDED_PORTALS=

# ----------------------------------------------------------------------------
# NOTAS IMPORTANTES
# ----------------------------------------------------------------------------
# - El archivo .env NO debe subirse a Git (ya está en .gitignore)
# - Valores sensibles (API keys, passwords) deben mantenerse seguros
# - Para producción, considerar usar variables de entorno del sistema
# - Documentación completa en: INSTALL.md
# ============================================================================
